doctype html
html
    head
    body
        input(type="text" name="inputLogin" value=`${ 1 > 2 ? "Ok" : "Not ok" }`)
        input(type="password" name="inputPwd" value=`${ 5 > 2 ? "Ok" : "Not ok" }`)


        - const myClasses = ["class1", "class2", "class3"] // javascript array
        div.additional-class(class=myClasses)
        // it adds all the classes to the div element

        - const myStyles = {"color": "red", "background-color": "blue"}
        div(style=myStyles) hey!

        - const myAttributes = { "src": "my-photo.png", "alt": "This is my photo"}
        img&attributes(myAttributes)

        input(type="text" disabled)
        input(type="text" disabled=false)
